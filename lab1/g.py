s = str(input())
index = len(s) - 1 #отсчет начинается с нуля, поэтому отнимаем 1

def ToDecimal(n):
    global s #увеличиваем видимость переменной s, чтобы она была видна и в других функциях

    if n < 0:
        return 0
    else:
        return (pow(2, (len(s) - 1 - n)) * (ord(s[n]) - 48)) + ToDecimal(n - 1) #отсчет для перевода с байнари на десимал осуществляется справо налево
        #индекс идет слева направо
        #0 1 2 ... - индекс

print (ToDecimal(index))